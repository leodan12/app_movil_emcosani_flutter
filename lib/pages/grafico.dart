

import 'package:flutter/material.dart';

class Grafico extends StatelessWidget {
  const Grafico({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("estas en las vista del Grafico de Inventarios"),
    );
  }
}